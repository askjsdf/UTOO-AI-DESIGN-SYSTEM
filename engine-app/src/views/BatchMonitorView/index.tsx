export default function BatchMonitorView() {
  return (
    <div className="h-full flex items-center justify-center" style={{ background: 'var(--bg-base)' }}>
      <p className="text-xs" style={{ color: 'var(--text-muted)' }}>批量任务监控 — Phase 4 实现</p>
    </div>
  )
}
